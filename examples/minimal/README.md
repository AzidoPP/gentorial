# Gentorial minimal example

阶段 1 的纵向贯通示例：静态 HTML 中始终存在 `switch` 概念锚点；标题旁的低干扰按钮使用学习者选择的叙事偏好，在原文后通过确定性 mock 直出经过协议校验的课程块，不附加 mock、示例等标签、标记或容器装饰。每个结果末尾常驻带 placeholder 的追问输入和发送按钮，后续回答仍受同一章节范围与概念锚点约束，并且只把回答课程块显示在输入组合上方。示例同时覆盖无显式概念引用的“C 的历史”章节范围。

```bash
pnpm build
pnpm dev
```
